# Local dev: where the Next app proxies /api/dashboard/* to.
# Copy this file to .env.local and adjust if your backend runs elsewhere.
# Defaults to http://localhost:4000 when unset (see next.config.js).
BACKEND_ORIGIN=http://localhost:4000
